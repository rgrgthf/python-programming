# ============================================================
# Excel 自动化 ③ — 实战：批量处理实验记录
# ============================================================
# ⚠️ 请在 sci 环境运行
# 综合实战：一个文件夹里 50 个 Excel 实验记录，
# 批量读取 → 汇总 → 统计 → 生成一张总表 + 统计报告。

import pandas as pd
import os
from pathlib import Path
from openpyxl import Workbook

# ============================================================
# 一、准备演示数据（模拟 5 个批次的 Excel 文件）
# ============================================================
Path("批量实验").mkdir(exist_ok=True)

for batch in range(1, 6):
    wb = Workbook()
    ws = wb.active
    ws.title = "记录"
    ws.append(["批次", "样品", "浓度", "吸光度"])
    for i in range(1, 6):
        ws.append([batch, f"B{batch}-{i}", i * 0.4, i * 0.2 + 0.02])
    wb.save(f"批量实验/批次{batch}_记录.xlsx")

print("已生成 5 个批次文件")

# ============================================================
# 二、批量读取所有 Excel
# ============================================================
all_data = []
for file in Path("批量实验").glob("*.xlsx"):
    df = pd.read_excel(file)
    all_data.append(df)
    print(f"读取 {file.name}: {len(df)} 条")

# 合并所有批次
combined = pd.concat(all_data, ignore_index=True)
print(f"\n共合并 {len(combined)} 条记录")
print(combined.head())

# ============================================================
# 三、批量统计（按批次分组）
# ============================================================
# 每组均值/标准差/样本数
stats = combined.groupby("批次").agg(
    样品数=("样品", "count"),
    平均浓度=("浓度", "mean"),
    平均吸光度=("吸光度", "mean"),
    吸光度标准差=("吸光度", "std"),
).round(3)
print("\n各批次统计：")
print(stats)

# ============================================================
# 四、合并后的全局统计
# ============================================================
overall = {
    "总记录数": len(combined),
    "总平均浓度": round(combined["浓度"].mean(), 3),
    "总平均吸光度": round(combined["吸光度"].mean(), 3),
    "吸光度最大值": round(combined["吸光度"].max(), 3),
    "吸光度最小值": round(combined["吸光度"].min(), 3),
}
print("\n总体统计：", overall)

# ============================================================
# 五、导出汇总报告
# ============================================================
with pd.ExcelWriter("批量实验_汇总报告.xlsx") as writer:
    combined.to_excel(writer, sheet_name="全部数据", index=False)
    stats.to_excel(writer, sheet_name="分批次统计")
    pd.DataFrame([overall]).to_excel(writer, sheet_name="总体统计", index=False)

print("\n✅ 汇总报告已生成：批量实验_汇总报告.xlsx")

# ============================================================
# 六、异常检查（自动发现问题）
# ============================================================
# 检查是否有异常值（超过3倍标准差）
mean = combined["吸光度"].mean()
std = combined["吸光度"].std()
outliers = combined[abs(combined["吸光度"] - mean) > 3 * std]
if len(outliers) > 0:
    print(f"\n⚠️ 发现 {len(outliers)} 个异常值：")
    print(outliers)
else:
    print("\n✅ 未发现异常值")

# ============================================================
# 七、总结：这个实战用到了什么？
# ============================================================
# Path.glob("*.xlsx")        → 批量找文件
# pd.read_excel              → 读每个文件
# pd.concat                  → 合并所有批次
# groupby().agg()            → 分组统计
# ExcelWriter + 多sheet      → 生成汇总报告
# 3倍标准差                 → 异常值检测
#
# 这就是"实验数据自动化处理"的完整套路，
# 毕业论文数据处理、实验课报告都能这么干！

# ============================================================
# 六、易错点汇总
# ============================================================
# 1. 批量处理多文件：用 Path.glob 收集文件，循环处理，
#    别一个个手动写
# 2. 每个文件的路径/表名要参数化，别写死
# 3. 批量处理中一个文件出错不能中断全部：
#    用 try/except 捕获并记录，继续下一个
# 4. 输出文件命名要规范（含源文件名/日期）
# 5. 处理前后打印进度，长任务可加日志
# 6. 先在小样本上测试，再全量跑

# ============================================================
# 七、自测（40%基础 + 40%中等 + 20%挑战）
# ============================================================
# 【基础】
# 1. 批量处理多个文件，用什么收集文件？
# 2. 一个文件出错为什么要 try/except 而不是中断？
# 3. 批量处理前为什么要先小样本测试？
#
# 【中等】
# 4. 遍历某文件夹所有 .xlsx，读取第一个 sheet 并汇总。
# 5. 给批量处理加错误捕获和进度打印。
# 6. 输出文件按"源文件名+日期"命名。
#
# 【挑战】
# 7. 写批量处理实验记录的完整脚本（含异常处理和日志）。
# 8. 解释为什么批量脚本要健壮（能跑完不崩）。
