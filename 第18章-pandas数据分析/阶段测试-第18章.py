# ============================================================
# 第九章 阶段测试 — pandas 与 matplotlib
# ============================================================
# 共 10 题：读代码、简答、编程（在 sci 环境运行）
# ============================================================


# ========== 一、pandas 基础 ==========

# 1.【读代码】写出输出：
import pandas as pd
df = pd.DataFrame({"组别": ["A", "B", "A", "B"],
                   "值": [10, 20, 30, 40]})
print(df.shape)
print(df["值"].mean())
print(df[df["组别"] == "A"]["值"].tolist())
# 你的答案：


# 2.【简答】pandas 的 DataFrame 和 numpy 的数组有什么区别？
# 你的答案：


# 3.【读代码】写出输出：
df = pd.DataFrame({"x": [1, 2, 3]})
df["y"] = df["x"] * 2
print(df["y"].sum())
print(df.loc[1, "y"])
# 你的答案：


# ========== 二、数据清洗 ==========

# 4.【读代码】写出输出（dropna 和 fillna）：
df = pd.DataFrame({"a": [1, None, 3], "b": [4, 5, 6]})
print(df.isna().sum().tolist())
print(df.fillna(0)["a"].tolist())
# 你的答案：


# 5.【简答】为什么读取中文 CSV 要写 encoding="utf-8" 或 "utf-8-sig"？
# 你的答案：


# ========== 三、分组统计 ==========

# 6.【编程】给定数据：
#   组别 = 对照*3 + 给药*3，值 = [95, 93, 97, 50, 45, 48]
#   用 groupby 输出每组的均值±标准差（保留2位小数）。
print("第6题（你的代码）：")
# 你的代码：


# ========== 四、文件读写 ==========

# 7.【编程】把下面数据存成 CSV（index=False），再读回打印：
#   {"浓度": [1, 2, 4], "吸光度": [0.1, 0.2, 0.4]}
print("第7题（你的代码）：")
# 你的代码：


# ========== 五、matplotlib ==========

# 8.【简答】画图前 plt.rcParams["font.sans-serif"] = ["SimHei"] 是干嘛的？
#     不设置会怎样？
# 你的答案：


# 9.【编程】画一个柱状图：三组 [对照, 低剂量, 高剂量] 的均值
#   [94.75, 79.0, 46.0]，带误差线，保存为 png。
print("第9题（你的代码）：")
# 你的代码：


# 10.【综合编程】完整流程：
#    读 CSV（自己生成一个含 组别/浓度 的csv）→ 按组统计均值
#    → 画柱状图 → 导出统计结果 Excel。
#    用第7题的思路 + groupby + matplotlib。
print("第10题（你的代码）：")
# 你的代码：
