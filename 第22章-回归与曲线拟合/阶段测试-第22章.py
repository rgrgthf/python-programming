# ============================================================
# 第十二章 阶段测试 — 回归与曲线拟合
# ============================================================
# 共 10 题：读代码、简答、编程（sci 环境）
# ============================================================
import numpy as np
import matplotlib.pyplot as plt
from scipy import stats
from scipy.optimize import curve_fit

# ========== 一、线性回归 ==========

# 1.【读代码】写出输出：
conc = np.array([0, 1, 2, 3, 4])
abs_ = np.array([0.01, 0.21, 0.41, 0.61, 0.81])
slope, intercept, r, p, se = stats.linregress(conc, abs_)
print(f"斜率: {slope:.2f}")
print(f"截距: {intercept:.2f}")
print(f"R²: {r**2:.3f}")
# 你的答案：


# 2.【简答】线性回归的 R² 表示什么？标准曲线要求 R² 通常达到多少？
# 你的答案：


# 3.【编程】用标准曲线 y = 0.5x + 0.02 的模型，
#     计算吸光度为 0.52 的未知样品浓度（公式 x = (y-b)/k）。
print("第3题（你的代码）：")
# 你的代码：


# ========== 二、非线性拟合 ==========

# 4.【读代码】写出输出：
def hill(x, bottom, top, ic50, h):
    return bottom + (top - bottom) / (1 + (x / ic50) ** (-h))

# 给定数据
xdata = np.array([0.1, 1, 10, 100])
ydata = np.array([5, 20, 60, 90])
popt, pcov = curve_fit(hill, xdata, ydata, p0=[0, 100, 10, 1])
print(f"IC50: {popt[2]:.2f}")
# 你的答案：


# 5.【简答】为什么剂量-反应曲线要用非线性拟合而不是线性回归？
# 你的答案：


# 6.【简答】curve_fit 的 p0（初始猜测值）作用是什么？不给会怎样？
# 你的答案：


# ========== 三、综合实战 ==========

# 7.【编程】给出一组标准曲线数据：
#   浓度: [0, 0.1, 0.2, 0.4, 0.8, 1.6]
#   吸光: [0.001, 0.082, 0.158, 0.331, 0.654, 1.302]
#   ① 线性拟合，打印方程和 R²
#   ② 计算吸光度 0.35、0.75 对应的浓度
print("第7题（你的代码）：")
# 你的代码：


# 8.【编程】模拟剂量反应数据并拟合 IC50：
#   浓度(logspace -1~2, 8个点)，抑制率依次 [2, 5, 15, 35, 60, 80, 92, 97]
#   用 Hill 方程拟合，打印 IC50。
print("第8题（你的代码）：")
# 你的代码：


# 9.【编程】画标准曲线图：
#   用第7题数据，散点 + 拟合直线，标注 R²，保存 png。
print("第9题（你的代码）：")
# 你的代码：


# 10.【综合】写一个函数 standard_curve(conc, abs_, unknown_abs_list)：
#    拟合标准曲线，返回 (斜率, 截距, R², 未知样品浓度列表)。
print("第10题（你的代码）：")
# 你的代码：
