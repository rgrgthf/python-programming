# ============================================================
# 第十一章 阶段测试 — scipy 科研统计
# ============================================================
# 共 10 题：读代码、简答、编程（sci 环境）
# ============================================================
import numpy as np
from scipy import stats

# ========== 一、基础 ==========

# 1.【读代码】写出输出：
data = np.array([1, 2, 3, 4, 5])
print(data.mean())
print(data.std())
print(stats.shapiro(data)[1] > 0.05)   # p值是否>0.05
# 你的答案：


# 2.【简答】shapiro 检验的 p > 0.05 意味着什么？
# 你的答案：


# ========== 二、t 检验 ==========

# 3.【读代码】写出输出：
a = np.array([10, 12, 11, 13])
b = np.array([20, 22, 21, 23])
t, p = stats.ttest_ind(a, b)
print(f"p = {p:.3f}")
print("差异显著" if p < 0.05 else "差异不显著")
# 你的答案：


# 4.【简答】独立样本 t 检验和配对 t 检验有什么区别？
#     分别什么场景用？
# 你的答案：


# ========== 三、ANOVA ==========

# 5.【读代码】写出输出：
g1 = np.array([95, 93, 97, 94])
g2 = np.array([80, 78, 82, 79])
g3 = np.array([50, 45, 47, 52])
f, p = stats.f_oneway(g1, g2, g3)
print(f"F = {f:.2f}")
print(f"p = {p:.3f}")
print("有差异" if p < 0.05 else "无差异")
# 你的答案：


# 6.【简答】ANOVA 显著后，为什么还需要做事后检验（post-hoc）？
# 你的答案：


# ========== 四、卡方与相关 ==========

# 7.【读代码】写出输出：
table = np.array([[30, 10], [35, 5]])
chi2, p, dof, exp = stats.chi2_contingency(table)
print(f"p = {p:.4f}")
# 你的答案：


# 8.【简答】卡方检验适用于什么类型的数据？数值数据（如浓度）能用卡方吗？
# 你的答案：


# 9.【读代码】写出输出：
x = np.array([1, 2, 3, 4, 5])
y = np.array([2, 4, 6, 8, 10])
r, p = stats.pearsonr(x, y)
print(f"r = {r:.2f}")
# 你的答案：


# ========== 五、综合编程 ==========

# 10.【综合编程】三组实验数据（对照/低剂量/高剂量）：
#    对照: [95, 93, 97, 94, 96]
#    低:   [80, 78, 82, 79, 81]
#    高:   [50, 45, 47, 52, 48]
#    ① 打印三组的均值±标准差
#    ② 做正态性检验（shapiro）
#    ③ 做 ANOVA，判断是否显著
#    ④ 若显著，用 ttest_ind 找出哪两组之间差异显著
print("第10题（你的代码）：")
# 你的代码：
